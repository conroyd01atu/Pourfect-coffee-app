public partial class App : Application
{
    public App(PourfectDatabase db)
    {
        InitializeComponent();

        // Inject the database into the first Shell-based page
        MainPage = new AppShell();
    }
}