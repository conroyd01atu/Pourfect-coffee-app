<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Pourfect.Views"
       x:Class="Pourfect.AppShell">

    <TabBar>

        <ShellContent 
            Title="Log Brew" 
            ContentTemplate="{DataTemplate views:BrewEntryPage}" 
            Icon="log.png" />

        <ShellContent 
            Title="History" 
            ContentTemplate="{DataTemplate views:BrewListPage}" 
            Icon="history.png" />

        <!-- More tabs will be added later -->

    </TabBar>

</Shell>